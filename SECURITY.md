# Security

Do not commit:
- API keys, tokens, connection strings
- participant-identifiable or raw wellbeing data
- Azure Functions local.settings.json

Use environment variables / .env files locally.
